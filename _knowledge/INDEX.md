# Knowledge Base

- **architecture/** — Project structure, build setup, design decisions, testing philosophy
- **lexer/** — JFlex lexer, token types, syntax highlighting, incremental lexing
- **parser/** — GrammarKit BNF, PSI tree, error recovery, reference implementations
